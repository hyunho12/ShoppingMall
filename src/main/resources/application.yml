spring:
  datasource:
    url: jdbc:mysql://localhost:3306/khh_db?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: netcube
    password: asdfasdf
    driver-class-name: com.mysql.cj.jdbc.Driver

  mail:
    host: smtp.gmail.com
    username: hyundho12@gmail.com
    password: voihqvcvrjqnrkfk
    smtp:
      socketFactory:
        port: 465
        class: javax.net.ssl.SSLSocketFactory
        fallback: false

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  #org.hibernate.type: trace