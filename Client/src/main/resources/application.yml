server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/khh_db?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: netcube
    password: asdfasdf
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
      show_sql: true



logging.level:
  org.hibernate.SQL: debug
  #org.hibernate.type: trace